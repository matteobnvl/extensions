{
  "name": "betclic-ai",
  "version": "1.0.0",
  "description": "Assistant IA de paris sportifs pour Betclic",
  "type": "module",
  "scripts": {
    "build": "vite build && vite build --config vite.content.config.js && vite build --config vite.background.config.js",
    "dev": "concurrently \"vite build --watch\" \"vite build --watch --config vite.content.config.js\" \"vite build --watch --config vite.background.config.js\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "vite": "^8.0.10"
  },
  "dependencies": {
    "@alpinejs/csp": "^3.15.12"
  }
}
