{
  "name": "betclic-ai",
  "version": "1.0.0",
  "description": "Assistant IA de paris sportifs pour Betclic",
  "type": "module",
  "scripts": {
    "build": "vite build && ENTRY=content vite build && ENTRY=background vite build",
    "dev": "concurrently \"vite build --watch\" \"ENTRY=content vite build --watch\" \"ENTRY=background vite build --watch\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "vite": "^8.0.10"
  },
  "dependencies": {
    "@alpinejs/csp": "^3.15.12"
  }
}
